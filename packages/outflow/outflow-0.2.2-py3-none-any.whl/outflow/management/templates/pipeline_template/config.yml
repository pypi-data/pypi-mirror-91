databases:
  default:
    dialect: sqlite
    path: outflow.db

## Task parameters can be used to specify runtime value for task in the config file

# tasks:
#   my_task:
#     parameters:
#       my_param1: param_value1

## The ray backend is recommended, the 'default' one is not suitable for parallel and/or distributed computing
backend: default

## Set the Slurm cluster config
# cluster:
#   num_nodes: 2
#   mem_per_node: 1GB
#   cpu_per_node: 1
