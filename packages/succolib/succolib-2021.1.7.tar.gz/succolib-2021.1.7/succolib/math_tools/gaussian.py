import numpy as np

########################################################################################################################

def fGaus(
        x,
        A,
        u,
        sigma
):

    expo = -(x-u)**2/(2*sigma**2)
    return A*np.exp(expo)
