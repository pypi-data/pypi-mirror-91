[tool:pytest]
addopts = --cov=. tests/

[coverage:run]
include = faker-events/*
omit = 
	env/*
	.env/*
	venv/*
	tests/*
	setup.py

[coverage:xml]
output = coverage.xml

[coverage:report]
fail_under = 30

[egg_info]
tag_build = 
tag_date = 0

