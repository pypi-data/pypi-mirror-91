[tool.poetry]
name = "pwninit"
version = "0.1.0"
description = "pwnable challenge quick starter."
authors = ["ICEB3AR <!dong992016>"]

[tool.poetry.dependencies]
python = "^2.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
pwninit = 'entry:main'