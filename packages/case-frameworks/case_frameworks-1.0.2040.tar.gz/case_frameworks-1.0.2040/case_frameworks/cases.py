# Cases includes classes containing frameworks and practise cases

# Imports useful python functions
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import scipy as sc
import sklearn as skl
import csv as csv
import openpyxl as pyxl
import pathlib
import os
import pydrive
import random as rd


class frameworks:
    """frameworks displays a variety of case frameworks depending on the function call.
    """


class practise:
    """practise enables the user to practise the case recorded in this class. Each case is an independent function.
    """